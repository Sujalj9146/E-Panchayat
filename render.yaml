services:
  - type: web
    name: e-panchayat-ai
    runtime: static
    buildCommand: npm run build
    publishDir: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
